spring:
  config:
    import: optional:file:/config/secrets/application-secrets.yaml

  cloud:
    nacos:
      server-addr: ${NACOS_ADDR:nacos:8848}

      discovery:
        server-addr: ${NACOS_ADDR:nacos:8848}

      config:
        server-addr: ${NACOS_ADDR:nacos:8848}